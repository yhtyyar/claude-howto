name: pre-commit
description: Run pre-commit checks
type: pre-commit

config:
  fail_fast: true
  timeout: 180
  parallel: true

steps:
  - id: lint
    name: Lint check
    type: command
    config:
      command: echo
      args: ["Linting passed"]
